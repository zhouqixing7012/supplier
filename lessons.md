# Lessons

- 原型页面只展示真实用户会看到的字段、按钮和必要提示；产品逻辑、内部处理流程、评审说明不要放进用户页面。
- 如需保留多场景演示能力，使用隐藏的原型条件或直接路由，不在正式页面增加“评审快捷入口”。
- 协同门户统一优先使用 shadcn/ui 组件源码模式和 Sonner Toast，不再混用 Ant Design，减少样式覆盖和组件体系冲突。
- 安全类“需处理但非系统故障”的登录提示优先使用橙色 Warning Toast（浅橙背景、橙色图标和边框），避免默认白色 Toast 或过重的错误态样式。
- 需要用户持续看到并操作的页面切换入口，应放在具体业务页面内部，而不是入口组件外层，避免路由或部署版本导致入口不可见。
- 多模块共用全局样式时，`body` 不应使用某个页面专属背景；页面专属背景必须放在对应页面容器上，避免登录页主题污染注册页。
- 高保真复刻任务优先使用纯 HTML/CSS 和固定像素布局，先逐张对齐截图的位置、间距、字号和边框，再考虑组件化；不要为了统一组件体系牺牲还原度。
- 步骤条等短文本必须显式设置 `white-space: nowrap`，避免“预览提交”等文案因容器宽度变化发生异常换行。

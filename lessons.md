# Lessons

- 原型页面只展示真实用户会看到的字段、按钮和必要提示；产品逻辑、内部处理流程、评审说明不要放进用户页面。
- 如需保留多场景演示能力，使用隐藏的原型条件或直接路由，不在正式页面增加“评审快捷入口”。
- 协同门户登录模块继续使用 shadcn/ui 组件源码模式和 Sonner Toast，避免在同一登录模块内混用组件体系。
- 安全类“需处理但非系统故障”的登录提示优先使用橙色 Warning Toast（浅橙背景、橙色图标和边框），避免默认白色 Toast 或过重的错误态样式。
- 需要用户持续看到并操作的页面切换入口，应放在具体业务页面内部，而不是入口组件外层，避免路由或部署版本导致入口不可见。
- 多模块共用全局样式时，`body` 不应使用某个页面专属背景；页面专属背景必须放在对应页面容器上，避免登录页主题污染注册页。
- 高保真复刻任务优先使用纯 HTML/CSS 和固定像素布局，先逐张对齐截图的位置、间距、字号和边框，再考虑组件化；不要为了统一组件体系牺牲还原度。
- 步骤条等短文本必须显式设置 `white-space: nowrap`，避免“预览提交”等文案因容器宽度变化发生异常换行。
- 用户说“在某字段后新增字段并顺排”时，按表单视觉阅读顺序处理：同一行先左后右，再进入下一行；新增字段占据紧邻的下一个位置，后续字段依次后移，每行最多两列。
- 带 hover 说明的帮助图标必须留在标签区，并为长标签预留足够宽度；Tooltip 优先向标签侧/上方展开，避免覆盖对应输入框。
- 对可持续迭代的产品原型，优先保留 React 实现；纯 HTML 高保真页只作为必要时的对照/兜底。
- 供应商代注册这类企业后台表单，用户明确偏好 Ant Design；注册模块优先使用 Ant Design 原生 Form、Tabs、Steps、Upload、Tooltip 等组件。
- Ant Design 默认组件直接拼装仍会有明显“组件库 Demo 感”；供应商后台页面应在 AntD 基础上做紧凑主题：减少大 Card/大上传区、固定 Label 宽度、控制内容最大宽度、弱化 Steps/Tabs、降低圆角和留白，让视觉更接近成熟企业后台。
- 供应商代注册页面保持纯白背景，不主动增加偏蓝/冷灰页面底色；层级通过边框、分隔线和留白实现，蓝色仅用于关键交互状态和主操作。
- 当页面级背景修改“看起来没生效”时，要同时检查全局 `body/html/#root` 背景和模块入口导入的全局 CSS；只改页面容器可能仍会在边缘、滚动或回弹区域透出全局底色。
- 页面“丑”时不要优先换组件库。先做 critique：检查层级、密度、对齐、组件 Demo 感、视觉噪音和主任务是否突出；再用 distill/layout/polish 精简现有设计。换库只能改变默认皮肤，不能自动建立好的视觉层级。
- 对成熟企业后台，Impeccable 方法要结合业务场景使用：重点采用“去模板感、避免 cards 套 cards、减少无意义装饰、保持一致节奏和明确主次”，而不是机械照搬营销网站的字体或表现性设计规则。
- Impeccable 已安装为项目级 skill。后续任何 UI/页面视觉相关任务，优先读取 `.github/skills/impeccable/SKILL.md`、对应 command reference、`PRODUCT.md` 和 `DESIGN.md` 后再动代码；不要只凭组件库默认样式或临时审美判断直接修改。
- 用户反馈“页面像缩放了”时，先确认是否真的存在 `transform: scale()` / `zoom`。最大宽度过小、字号和控件高度过度压缩、固定 `min-width` 导致局部裁切，也会形成整体被缩小的观感；修复应优先改为结构化响应式重排，而不是再做反向缩放。
- 响应式适配不能等同于缩放截图。宽屏应合理利用空间，桌面可两列，平板/手机通过断点改为单列；Tabs/Steps 应使用组件自身的响应式能力，不用固定最小宽度硬撑。
- 当用户明确要求在多个原型页面之间切换时，可以增加共享的产品级顶部导航；跨不同 UI 技术栈模块时，优先保持样式隔离，必要时允许模块级重载，而不是为了无刷新强行把 AntD reset 与另一套 UI 主题混在同一运行上下文。
